<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MultiApp.UserPage">
  <ContentPage.Content>
    <StackLayout>
      <Label x:Name="userName" FontSize="40" />
      <Label x:Name="userAcc" FontSize="24" />
      <Label x:Name="userMail" FontSize="24" />
      <Label Text="{Binding Items}" />
      <ListView x:Name="tasksListView"
                VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand"
                ItemsSource="{Binding Items}"  IsPullToRefreshEnabled="False" ItemTapped="OnItemTapped" >
        <ListView.ItemTemplate>
          <DataTemplate x:Name="tasksListViewDataTemplate">
            <TextCell />
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
      <Image Scale="0.3">
        <Image.Source>
          <UriImageSource Uri="https://image.freepik.com/free-icon/male-user-shadow_318-34042.png"/>
        </Image.Source>
      </Image>

    </StackLayout>
  </ContentPage.Content>
</ContentPage>